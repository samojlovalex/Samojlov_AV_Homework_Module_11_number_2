<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".LastActivity">

    <LinearLayout
        android:id="@+id/containerSeventhToolbarLL"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/padding"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="MissingConstraints">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbarLast"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/Blue"
            app:subtitleTextAppearance="@style/TextAppearance.AppCompat.Small"
            app:subtitleTextColor="@color/white"
            app:titleTextAppearance="@style/TextAppearance.AppCompat.Medium"
            app:titleTextColor="@color/white"
            tools:ignore="MissingConstraints">
        </androidx.appcompat.widget.Toolbar>

    </LinearLayout>
    <LinearLayout
        android:id="@+id/containerTextViewLastLL"
        android:layout_width="match_parent"
        android:layout_height="@dimen/layout_width"
        android:orientation="vertical"
        android:padding="@dimen/padding"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/containerSeventhToolbarLL"
        app:layout_constraintBottom_toBottomOf="parent"
        android:gravity="center"
        tools:ignore="MissingConstraints">

        <TextView
            android:id="@+id/textViewResultTV"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/lastResult"
            android:textSize="30sp"
            android:textStyle="bold"
            android:gravity="center">

        </TextView>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>